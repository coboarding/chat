<!DOCTYPE html>
<html>
<head>
    <title>{{ website_title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
            line-height: 1.6;
            color: #333;
        }
        h1 {
            color: #2c3e50;
        }
        .info {
            background-color: #f8f9fa;
            padding: 15px;
            border-radius: 5px;
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>{{ website_title }}</h1>
    <p>This page was deployed using Ansible!</p>
    
    <div class="info">
        <h2>Server Information:</h2>
        <ul>
            <li>Environment: {{ env }}</li>
            <li>Deployment Time: {% raw %}{{ ansible_date_time | default('now') }}{% endraw %}</li>
        </ul>
    </div>
</body>
</html>
